# Backend API base URL (no trailing slash)
# Local dev:  http://localhost:3000/api
# EC2 prod:   /api  (Nginx same-origin proxy — see deploy/env/frontend.env.production)
VITE_API_URL=http://localhost:3000/api
